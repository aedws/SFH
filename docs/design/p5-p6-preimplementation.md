---
title: P5·P6 사전 구현 설계
description: P4-06 이후 최신 GDD의 로비 허브·준비 세션과 비동기 시즌을 구현하기 전에 고정하는 모듈·데이터·플레이어 E2E 작업 계약
tags:
  - Phase 5
  - Phase 6
  - 사전 설계
  - 로비 투자
  - 랭킹
  - 시즌
---

# P5·P6 사전 구현 설계

!!! info "현행 상태 · P5와 P6-01~02 완료"
    이 문서는 P4-06 이후의 순서와 완료 조건을 고정합니다. P5-01~10과 P6-01~04 로컬 프로토타입은 코드·계약·플레이어 인식 E2E를 통과했습니다. 실서비스 서버·계정·검증 지급 운영은 미완료입니다.

## 다음 실행 순서

`P5-01 캐릭터 → 02 무기·스킬 → 03 유틸리티 → 04 초안·BEP → 05 파산 방지 → 06~08 상점·제작 → 09 훈련장 → 10 도감 → P6 비동기 시즌`

각 패킷은 `데이터 계약 → 순수 정책 → 서비스 → 반응형 Presenter → 플레이어 인식 E2E → 모듈 제거 검사` 순으로 끝냅니다. 새 목록이 필요할 때만 Google Sheet 탭/열을 확장하고, 실시간 시험과 확정 CSV 공급 구조는 유지합니다.

## Phase 5 · 로비 투자

| ID | 먼저 필요한 데이터 | 독립 모듈 | 플레이어 인식 완료 조건 | E2E |
|---|---|---|---|---|
| P5-01 · 완료 | Character ID·패시브·투입 가격·표현 키 | `CharacterDefinition`, `CharacterSelectionService`, Presenter | 캐릭터를 바꾸면 패시브와 총비용이 즉시 바뀜 | 선택→견적→출격→실전 패시브 |
| P5-02 · 완료 | Weapon·Skill 가격과 해금·태그 요구 | 기존 자산 정의 + `LoadoutInvestmentService` | 소유·미해금·이번 런 구매를 혼동하지 않음 | 구매→태그 검증→장착→발동→거점 복원 |
| P5-03 · 완료 | 가방·회복/공격 소모품·비상 탈출 항목 | `UtilityInvestmentService` | 수량·사용 조건·사망 소실을 진입 전 확인 | 선택→작전 사용→잔량→성공/사망 |
| P5-04 · 완료 | 선택 전체와 BEP 계산 항목 | 불변 `OperationDraftService` | 모든 선택·총비용·기대 보상이 한 브리핑에 표시 | 편집 중 무과금→한 번 확정→원자적 차감 |
| P5-05 · 완료 | 무료 캐릭터·무기·맵 프리셋 | `BankruptcyProtectionPolicy` | 재화 0에서도 무료 프리셋으로 반복 출격 | 0 C→출격→결말→재출격 |
| P5-06 · 완료 | ShopOffer 품질·가격·표현 키 | `RotatingShopService` | 손상·표준·고성능의 가격·성능 차이를 구매 전 확인 | 세 품질군→견적→구매→보관함 |
| P5-07 · 완료 | 회전 시드·리롤 가격·초기화 | `RotatingShopService` | 런 복귀 갱신과 골드 리롤 결과·잔액 확인 | 복귀→회전→리롤→중복 차감 방지 |
| P5-08 · 완료 | 설계도·레시피·옵션/소켓 범위 | `WorkshopService` | 반출 설계도를 영구 등록하고 맞춤 제작 | 반출→등록→재접속→제작→보관함 |
| P5-09 · 완료 | 더미 시나리오·계측 시간창 | `TrainingService` | 단일·밀집 시험과 DPS·타격·AP·쿨타임 확인 | 생성→공격→계측→자유 세팅→퇴장 복구 |
| P5-10 · 완료 | 도감 항목·진행·지역 힌트 | `CodexService` | 해금률과 미해금 지역 힌트를 로비에서 확인 | 반출→도감 증가→재접속→힌트 유지 |

### P5 데이터 요청 게이트

- P5-01에서 `Character` 시트 12열·3개 임시 행을 만들었으며 기획 확정 시 데이터만 교체합니다.
- P5-02에서 기존 Weapon·Skill 탭에 가격·기본 소유·요구 해금·근거 상태 열을 추가했습니다. 현재 80 C·60 C는 임시값입니다.
- P5-03~10 구현에서 `Utility`, `OperationPreset`, `ShopOffer`, `Recipe`, `TrainingScenario`, `Codex` 탭과 Item 유틸리티 4행을 추가했습니다. 모두 `provisional`이며 기획 확정 때 데이터만 교체합니다.
- 상점 매물·제작 레시피·훈련 시나리오·도감 목록이 실제 구현에 필요해질 때만 `ShopOffer`, `Recipe`, `TrainingScenario`, `Codex` 탭을 추가합니다.
- 기획 수치가 없으면 임시값은 `provisional` 태그와 근거를 함께 기록하고 확정값처럼 표시하지 않습니다.
- 과금·결제·유료 재화 모델은 이 작업선 범위가 아니며 변경하지 않습니다.

## Phase 6 · 비동기 랭킹과 시즌

| ID | 경계 | 플레이어 인식 완료 조건 | E2E/보안 게이트 |
|---|---|---|---|
| P6-01 · 완료 | `RankingProvider` 인터페이스 뒤 로컬·온라인 구현 교체 | 온라인/오프라인·동기화 대기 상태가 보임 | 공급자 교체, 네트워크 실패, 로컬 보존 |
| P6-02 · 완료 | `RankingIdentity`, `RunSubmissionEnvelope`, 멱등 키, 영구 검증 큐 | 제출 성공·재시도·거부 이유를 결과에서 확인 | 중복 제출, 변조, 시간초과, 저장·복원 재시도 |
| P6-03 · 완료 | `SeasonPolicy` + `SeasonRankingService` + `SeasonPresenter` | 검토 단계에서 종료 시각·참가 규격·읽기 전용 기록 확인 | 시즌 경계·규격 불일치·마감 불변·저장 복원·선택 제거 |
| P6-04 · 로컬 완료 | `SeasonRewardService` 지급과 프로필 장착·표현 분리 | 지급 근거와 칭호·오라 적용을 확인 | 순위 확정→1회 지급→재접속 유지 |

### P6 중단 조건

- 서버·계정·보안 제공자가 선택되기 전에는 실제 외부 제출이나 지급을 연결하지 않습니다.
- 로컬 랭킹은 항상 폴백으로 유지합니다.
- 결제 정보·과금 모델·Cloudflare 유료 기능을 새로 활성화하지 않습니다.
- 익명 기기 신원·무결성 다이제스트·멱등·재시도 계약은 완료됐습니다. 실서비스 로그인과 서버 비밀 서명은 공급자 선택 전까지 완료로 표시하지 않습니다.

## 공통 수락 기준

1. 데이터 제공자를 제거하거나 확정 CSV 모드로 바꿔도 기본 플레이가 유지됩니다.
2. 서비스는 화면 노드를 직접 찾지 않고 Signal과 읽기 전용 스냅샷을 제공합니다.
3. 브라우저·Windows 동일 산출물에서 선택→차감→작전 효과→결말→다음 세션을 검증합니다.
4. 실패·취소·중복 입력이 자산을 두 번 차감하거나 지급하지 않습니다.
5. 위키에는 완료·진행·기획 요청·임시값을 서로 다른 상태로 표시합니다.

## 관련 문서

- [현행 마일스톤 작업 라인](current-milestone-workline.md)
- [Master GDD 구현 대조](master-gdd-alignment.md)
- [기획 요청·밸런스 데이터 운영](planner-request-workflow.md)
- [런 전리품 정산](../features/run-settlement.md)
