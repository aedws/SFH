---
title: 방 진입 전투와 봉쇄 보상
description: 방 진입 시 적 생성, 출입문 봉쇄, 전멸 판정과 전투 데이터 보상으로 이어지는 모듈형 교전 흐름
tags:
  - 방 전투
  - 문 봉쇄
  - 적 생성
  - 섬멸
  - 보상
  - 로그라이크
  - 모듈
---

# 방 진입 전투와 봉쇄 보상

작전 맵의 일반 방 안쪽으로 진입하면 해당 방 전투가 시작됩니다. 적 무리가 방 내부의 걸을 수 있는 위치에 생성되고 모든 출입구가 물리적으로 닫힙니다. 생성된 적을 모두 처치하면 문이 열리고 방 중앙에 **전투 데이터 보상**이 나타납니다.

시작 방과 탈출 방은 기본적으로 교전 대상에서 제외합니다. 문턱에서 충돌체가 플레이어와 겹치지 않도록 방 경계에서 48px 안쪽에 들어왔을 때 전투를 확정합니다.

## 플레이 흐름

1. 통로에서 아직 확보하지 않은 일반 방으로 진입합니다.
2. 방 등급 정책에 따라 적 수를 무작위로 정하고, 남은 총 생성 예산 안에서 한 번에 배치합니다.
3. 적 생성이 성공한 경우에만 출입문에 봉쇄 장벽을 만듭니다.
4. 이 방이 요청한 적 인스턴스만 추적해 전멸을 판정합니다.
5. 전멸 시 장벽을 제거하고 방 중앙에 전투 데이터 보상을 생성합니다.
6. 보상을 직접 접촉해 회수하면 내부 경험치를 받습니다.

## 등급별 기본값

| 작전 | 방당 적 | 최대 교전 방 | 방 보상 내부 경험치 | 총 생성 한계 |
|---|---:|---:|---:|---:|
| 소형 | 6~8 | 12 | 4 | 120 |
| 중형 | 8~11 | 18 | 6 | 220 |
| 대형 | 10~14 | 24 | 8 | 360 |

최대 교전 수와 방당 수량의 상한을 곱해도 기존 `EnemySpawnTierConfig`의 총 생성 한계를 넘지 않게 설정했습니다. 적을 전부 처치해도 총 생성 예산을 소진하면 더 생성되지 않습니다.

## 모듈 구조

```text
RoguelikeMapGenerator
  ├─ get_room_encounter_snapshot()  # 방 경계·중심·출입구 사본
  └─ get_room_spawn_positions()     # 장애물을 피한 방 내부 위치

EnemySpawner
  ├─ spawn_enemy_at()               # 같은 적 구성 경로 재사용
  ├─ get_remaining_spawn_budget()   # 유한 생성 한계 유지
  └─ set_reinforcement_paused()     # 방 전투 사용 시 전역 증원 정지

RoomEncounterSystem
  ├─ 진입→생성→봉쇄→전멸→보상 상태 관리
  ├─ RoomDoorBarrier                # 충돌 레이어 16의 교체 가능한 문 표현
  └─ RoomRewardPickup               # 내부 경험치 보상 오브젝트
```

방 전투 모듈은 맵의 `rooms`, `floor_cells`, `obstacle_cells`를 직접 읽지 않습니다. 맵이 제공하는 복사된 방 스냅샷과 생성 위치 계약만 사용합니다. 적의 체력이나 사망 내부 구현도 읽지 않고 생성 인스턴스의 `tree_exited` 수명만 추적합니다.

## 활성화와 폴백

`FeatureManifest.room_encounters_enabled`로 독립 활성화합니다. 활성 상태에서는 `room_encounters` 출처가 기존 전역 증원을 정지시키고 방 진입 생성만 사용합니다. 이 기능을 끄면 `RoomEncounterSystem`, 문 장벽, 방 보상이 설치되지 않으며 기존 목표 동시 개체 수 기반 전역 증원이 그대로 복구됩니다.

설정은 `game/features/room_encounters/configs/default_room_encounters.tres`에서 바꿉니다. 방 전투는 `map_generation`과 `spawning`에 의존하며 Manifest가 잘못된 조합과 설정 경로를 시작 전에 차단합니다.

## 자동 검증

- 소·중·대형에서 방당 최소~최대 적 수
- 적 생성 뒤 문 장벽 충돌체 생성
- 해당 방 적 전멸 뒤 장벽 제거
- 방 중앙 보상 생성과 내부 경험치 지급
- 전체 생성 예산 유지
- 기능 비활성 시 기존 전역 증원 폴백
- 대형 방 적 14기, 전기 스킬 3개 동시 성능 예산

## 검색 별칭

엔터 더 건전, 방 잠금, 문 닫힘, 방 클리어, 룸 인카운터, 웨이브 룸, 몬스터 방, 전멸 보상, 전투 데이터, 방 보상
