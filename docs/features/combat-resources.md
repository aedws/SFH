---
title: 전투 에너지·충전·회복 드랍
description: AP 드랍·재생·충전과 소모품 전용 HP 정책의 분리
tags:
  - 에너지
  - 충전
  - 체력 회복
  - 적 드랍
  - 스킬 자원
  - 모듈
---

# 전투 에너지·충전·회복 드랍

스킬은 공용 에너지와 슬롯별 충전 횟수를 모두 만족해야 발동합니다. 2026-09-08부터 표준 HP는 [소모품 전용](health-recovery.md)이며 적 드랍으로는 AP만 보충합니다.

## 현재 전투 자원 규칙

| 항목 | 기본값 | 동작 |
|---|---:|---|
| 최대·시작 에너지 | 100 | 세 스킬이 공유하며 작전 시작 시 가득 참 |
| AP 자연 회복 · 임시 | 마지막 소모 후 1초 · 초당 10 | 지연 이후 경과분만 회복, 최대치 초과 금지. 기존 드랍·슬롯 충전과 병행 |
| 에너지 결정 | 적 처치당 55% · 20 회복 | 플레이어 근처에서 끌려오며 접촉 시 즉시 회복 |
| 체력 결정 | 표준 프로필 차단 | 기존 대체 프로필 수치는 보존하지만 표준 작전에서는 생성/회복하지 않음 |
| 최소 드랍 보정 | 처치당 1개 | 두 확률이 모두 실패하면 에너지 결정을 1개 생성 |
| 동시 드랍 | 표준 HP 결정 없음 | AP 최소 드랍 보정은 유지 |
| 레벨업 회복 | 표준 프로필 차단 | 성장·증강 선택은 유지, HP 회복 요청만 거부 |

충전 자원은 적 드랍이 아니라 시간으로 복구됩니다. 따라서 처치로 에너지를 확보하는 흐름과 스킬별 연속 사용 제한을 독립적으로 조정할 수 있습니다.

## 스킬별 소비량

| 키 | 스킬 | 에너지 | 최대 충전 | 충전 1회 복구 |
|---:|---|---:|---:|---:|
| `1` | 점멸 | 20 | 2 | 7초 |
| `2` | 원형 자기장 | 35 | 1 | 10초 |
| `3` | 기동 가속 | 25 | 2 | 14초 |

쿨타임, 에너지, 충전이 모두 준비됐을 때만 스킬을 사용할 수 있습니다. HUD는 굵은 전체 에너지 막대, 현재값·최대값, 백분율을 함께 표시합니다. 35% 이하는 `LOW`, 15% 이하는 `CRITICAL` 색으로 바뀌며 각 슬롯에서는 소비량·현재 충전 수와 `충전 대기` 또는 `에너지 부족` 원인을 구분합니다.

## 모듈 구조

2026-09-07 오너가 승인한 **임시 정책**입니다. 기획 최종 확정과 구분하며 HP 회복은 변경하지 않았습니다. `EnergyRegenerationPolicy` Resource의 `enabled`, `delay_after_spend`, `energy_per_second`를 조정하거나 회복량 계산 Resource를 교체합니다. Config에서 정책을 제거하면 기존 드랍/충전만 작동합니다. 새 게임 목록이 아니므로 Sheet·CSV·Web payload는 그대로 유지합니다.

정책은 회복량만 계산하고 `CombatResourceSystem`이 AP·대기 시간·충전을 소유합니다. 대기 시간도 훈련 스냅샷에 포함하며, 화면 알림은 표시되는 AP 정수값이 변할 때 갱신합니다.

```text
Enemy defeated Signal
  └─ CombatResourceSystem
       ├─ CombatResourceConfig Resource   # 최대량·드랍률·회복량·자석 범위
       ├─ CombatResourcePickup Scene      # 에너지/체력 표시·끌림·접촉 수집
       ├─ CombatSkillLoadout Resource     # 스킬별 비용·충전량·복구 시간
       └─ Player heal() 계약              # 체력 자원과 레벨업 회복의 적용 대상

CombatSkillSystem
  └─ can_activate / consume_for_skill 제공자 계약만 사용
```

- `CombatResourceSystem`은 적 체력·전리품·스킬 효과를 직접 알지 않고 사망 위치와 스킬 정의만 받습니다.
- 드랍 확률과 보정은 `default_combat_resources.tres`, 스킬 소비량과 충전은 각 스킬 `.tres`에서 교체합니다.
- 체력 결정은 플레이어의 `heal()` 공개 계약을 사용해 다른 체력 회복 수단과 중복 구현하지 않습니다.
- `ProgressionSystem`의 회복 요청은 `level_up` 원인으로 판정하며 표준 프로필에서는 거부됩니다.
- `combat_resources_enabled`를 끄면 결정과 에너지 HUD가 제거되고 기존 쿨타임 전용 스킬 폴백은 유지됩니다.

## 자동 검증

`provisional_policy_contract_test.gd`의 `PROVISIONAL_POLICY_OK`: 1초 경계, 프레임 분할과 큰 delta의 동일 회복량, 최대치, 정책 교체/제거, 대기 시간 복원·비정상 값 거부를 검사합니다. 기존 드랍/충전 검사는 자연 회복을 제거한 독립 설정에서도 유지합니다.

- 스킬 발동 시 에너지·충전 동시 차감과 부족 시 발동 차단
- 점멸 충전 7초 복구와 슬롯별 독립 충전
- 적 1회 처치에서 에너지·체력 결정 생성·수집·실제 수치 반영
- 레벨업 시 정확히 HP 12 회복
- 전투 자원 모듈 비활성화 시 쿨타임 전용 스킬 정상 작동
- HUD의 에너지 현재값·백분율·가용 상태와 충전 수 표시

## 검색 별칭

마나, MP, 스킬 게이지, 에너지 UI, 에너지 부족, LOW, CRITICAL, 에너지 결정, 체력 결정, 체력 드랍, HP 드랍, 적 처치 회복, 충전 횟수, 차지, 스킬 스택, 레벨업 회복, 레벨업 힐
