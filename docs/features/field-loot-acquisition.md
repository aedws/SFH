---
title: 현장 전리품 비교·획득
description: 방 보상 드랍에 접근해 현재 상태와 탈출·사망 결과를 비교하고 보류 또는 런 임시 보관으로 획득하는 P4-03 계약
tags:
  - 전리품
  - 현장 비교
  - 획득
  - E2E
  - 모듈화
---

# 현장 전리품 비교·획득

P4-03은 작전 중 전리품을 발견한 순간부터 선택 결과까지 연결합니다. 방을 확보하면 `LootTableProvider`가 작전 지역·난이도·규모와 `room_reward` 출처로 후보를 추첨하고, 월드에 청록색 비교 신호를 하나 생성합니다.

## 플레이 흐름

1. 방 전투를 끝내 문 봉쇄를 해제합니다.
2. 보상 박스와 별도로 `LOOT · G등급` 신호가 생성됩니다.
3. 가까이 가면 현재 무기, 후보 등급, 보유 수량, 이번 런 사용 방식, 탈출·사망 결과가 표시됩니다.
4. `ESC`는 패널만 닫고 전리품을 월드에 유지합니다.
5. 다시 접근해 `F`를 누르면 전리품이 런 임시 보관으로 이동합니다.

P4-03은 장비를 즉시 교체하지 않습니다. 실제 무기·방어구·스킬 교체와 기존 항목 처리는 다음 P4-04가 소유합니다.

## 모듈 경계

| 모듈 | 소유 책임 | 알지 못하는 것 |
|---|---|---|
| `LootTableProvider` | 작전 문맥 후보와 결정적 추첨 | 월드 노드·UI·획득 상태 |
| `LootLifecycleService` | 아이템 의미와 탈출·사망 결과 | 거리·입력·장비 교체 |
| `FieldLootComparisonService` | 후보와 현재 장비·보유 수량의 읽기 전용 비교 | 월드 생성·입력·장착 명령 |
| `FieldLootDrop` | 위치·접근 거리·신호 표현 | 생명 주기·인벤토리 변경 |
| `FieldLootComparisonPanel` | 비교 결과와 F/ESC 단서 표시 | 추첨·획득 규칙 |
| `FieldLootAcquisitionService` | 접근 초점·보류·런 임시 보관 | P4-04 장비 교체·P4-06 정산 |

Manifest에서 `field_loot_acquisition_enabled`만 끄면 생명 주기와 지역 드랍 브리핑은 유지되고 월드 비교·획득만 빠집니다.

## 자동 검증

- 계약 테스트: 생성→접근→비교→ESC 보류→월드 유지→재접근→선택→런 보관
- 플레이어 인식: 이름·등급·보유 수량·현재 무기·탈출·사망·F/ESC 단서
- 실제 입력 E2E: 방 확보 뒤 물리 ESC와 F를 사용해 25개 체크포인트·7개 인과 흐름 연속 판정
- 모듈 제거: 현장 획득을 꺼도 `LootTableProvider`와 `LootLifecycleService`가 유지되는지 확인

이번 작업은 기존 Item 15개와 LootTable 27행을 조합하므로 새 데이터 목록이 필요하지 않았습니다. 이후 신규 열거형 목록이 필요하면 [기획 요청·밸런스 데이터 운영](../design/planner-request-workflow.md)의 상시 허용 규칙에 따라 Google Sheet와 확정 CSV를 함께 확장합니다.

## 검색 별칭

현장 드랍, 아이템 비교, 전리품 접근, F 획득, ESC 보류, 런 임시 보관, P4-03, 장비 비교 패널
