---
title: P8 훈련 연습장
description: 거점에서 단일 보스·밀집 더미를 실제 공격하고 초기화하는 P8-01 플레이와 모듈 계약
tags:
  - P8
  - 훈련장
  - 전투 테스트
  - 모듈화
  - E2E
---

# P8 훈련 연습장

훈련장은 작전 보상·처치 수·일반 적 스폰 예산을 건드리지 않고 현재 장비의 전투 동작을 확인하는 거점 시설입니다. 플레이어는 거점의 **훈련장 단말에 F로 상호작용**해 다음 시나리오로 전환합니다.

| 시나리오 | 현재 임시 데이터 | 확인 목적 |
|---|---|---|
| 단일 중장 더미 | 1기 · HP 1,000 · 방어 25 | 보스 우선 타게팅과 단일 대상 피해 |
| 밀집 경량 더미 | 8기 · HP 240 · 방어 8 | 겹치지 않는 배치와 밀집 중심 자동 타게팅 |

두 행은 기존 `TrainingScenario` CSV를 그대로 사용합니다. 새 목록이 생기지 않아 Google Sheet는 확장하지 않았고, 수치는 `provisional`로 남겨 기획 확정 CSV 교체가 가능하게 했습니다.

## 플레이 흐름

1. 시작 거점에서 훈련장 단말로 이동합니다.
2. `F`로 단일 중장 더미를 생성합니다. 다시 상호작용하면 밀집 경량 더미로 전환됩니다.
3. 실제 좌클릭 공격과 스마트 자동 타게팅이 더미에게 적용됩니다.
4. 모든 더미를 처치하면 짧은 지연 뒤 같은 시나리오가 자동 초기화됩니다.
5. 거점을 나가면 더미와 훈련 전용 무기가 함께 제거됩니다.

훈련 더미는 경험치·드랍·처치 통계·방 보상·일반 적 생성 한도를 증가시키지 않습니다. 거점 밖 작전의 적 생성기와 전리품 생명 주기를 참조하지 않는 별도 경계입니다.

더미 생성 기준점은 훈련 단말에서 325px 떨어진 우측 시험 구역으로 옮겼습니다. 단말과 표적이 겹쳐 어느 대상을 상호작용하는지 모호했던 상태를 제거했습니다. 실제 피해가 들어오면 거점 상태 줄에 `훈련 타격 · 최근 피해 · 누적 피해 · 타격 수`가 즉시 바뀝니다. 이 즉시 확인은 P8-01 조작 피드백이며, 시간창 DPS·AP/s·쿨타임 비교를 제공하는 P8-02 전용 HUD와는 구분합니다.

## 모듈 계약

| 모듈 | 한 가지 책임 | 교체 지점 |
|---|---|---|
| `TrainingScenarioDefinition` | CSV 행 검증과 런타임 스냅샷 | 수치·시나리오 종류 |
| `TrainingDummySpawner` | 더미 소유·배치·생성·제거 | 더미 Scene·배치 알고리즘 |
| `TrainingScenarioResetService` | 수동/자동 초기화와 revision | 재생성 지연·정책 |
| `TrainingGroundService` | 정의·생성·초기화 조립 façade | UI·Game이 호출하는 공개 계약 |
| `P5HubProgressionService` | 기존 훈련 목록·세션 신호 제공 | CSV 공급자 |
| `Game` | 거점 단말·실제 무기·피해 신호 연결 | Scene 조립만 담당 |

`FeatureManifest.training_ground_enabled`를 끄면 P5·거점·적·무기·장비 기능을 유지한 채 훈련장만 제거됩니다. 선행 기능이 비활성인 빌드에서는 훈련장을 자동 제외하므로 선택적 빌드가 전체 Manifest 오류로 바뀌지 않습니다.

## 검증

- `P8_TRAINING_GROUND_OK`: Resource 검증, 단일 보스, 밀집 8기, 최소 간격, 밀집 타게팅, 수동·자동 초기화, 스폰 예산 비간섭, 선택 제거를 판정합니다.
- `E2E_P8_TRAINING_GROUND_OK`: 실제 `Game` 거점에서 단말 상호작용→단말과 분리된 단일 생성→좌클릭 피해·즉시 화면 피드백→밀집 전환→거점 이탈 정리를 판정합니다.
- 전체 모듈 감사: 기능 57개, 클래스 218개, 의존 26개, 순환 0, 서비스의 Scene 직접 참조 0, P8 경계 4개입니다.

다음 작업 `P8-02`는 기존 타격 기록을 전용 수집기·시간창·HUD Presenter로 분리해 DPS·단일 타격·AP/s·쿨타임을 한 화면에서 읽게 하는 단계입니다. `P8-03`의 무비용 자유 세팅은 그 다음이며 영구 자산을 변경하지 않는 스냅샷 복구가 완료 조건입니다.

## 관련 문서

- [P5 거점 진행](p5-hub-progression.md)
- [P7 이후 작업 설계](../design/p7-plus-preimplementation.md)
- [플레이 세션 E2E](../quality/e2e-play-session.md#p8-training-ground-e2e)
- [모듈 감사](../architecture/module-audit.md#p8-01-2026-09-04)

훈련장, 단일 더미, 밀집 더미, 보스 더미, P8-01, 자동 초기화, 스마트 타게팅, 스폰 예산, TrainingScenario, TrainingGroundService
